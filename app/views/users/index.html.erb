<h1 class="index-title">Here is a List of all Users</h1>
<ul class="user-list">
  <% @users.each do |user| %>
    <li class="user-card flex" id="user-card-<%= user.id %>">
      <div class="user-image border">
        <img src="<%= user.photo %>" alt="<%= user.name %>'s photo" width="100% heigth="100%">
      </div>
      <div class="user-info border">
        <h2><%= link_to user.name, user_path(user) %></h2>
        <p class="posts-number">Number of posts: <%= user.posts.count %></p>
      </div>
    </li>
  <% end %>
</ul>

<a href="/users/1/posts/new">New Post</a>

